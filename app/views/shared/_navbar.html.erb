
<div>
  <nav class="bg-gradient-to-r from-green-700 to-blue-700">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="flex items-center justify-between h-16">
        <div class="flex items-center">
          <div class="flex-shrink-0">
          <div class='text-2xl text-white'>Segundo Parcial </div>
            
          </div>
          <div class="hidden md:block">
            <div class="ml-10 flex items-baseline space-x-4">
            <%= link_to 'Centros de Salud', medical_centers_path, :class => "ml-3 relative bg-green-900 text-white px-3 py-2 rounded-md font-medium hover:bg-green-800 focus:bg-gray-700" %>
            <%= link_to 'Trabajadores', workers_path, :class => "ml-3 relative bg-green-900 text-white px-3 py-2 rounded-md font-medium hover:bg-green-800 focus:bg-gray-700" %>
            <%= link_to 'Panel de Guardias', guards_path, :class => "ml-3 relative bg-green-900 text-white px-3 py-2 rounded-md font-medium hover:bg-green-800 focus:bg-gray-700" %>
            <%= link_to 'Turnos', appointments_path, :class => "ml-3 relative bg-green-900 text-white px-3 py-2 rounded-md font-medium hover:bg-green-800 focus:bg-gray-700" %>
               <%#= select("post", "category_id", Category.where(category_id: nil).pluck(:name, :id), {:include_blank =>  'All Categories',:selected => params[:search]}, :onchange => 'window.location= `/categories?search=${document.getElementById("post_category_id").value}`', class: "ml-3 relative bg-gray-800 text-white px-3 py-2 rounded-md font-medium hover:bg-gray-900 focus:bg-gray-700") %>
               <%# <% if current_user.admin? %>
                <%= link_to 'Admin Panel', administrators_path, :class => "ml-3 relative bg-green-900 text-white px-3 py-2 rounded-md font-medium hover:bg-green-800 focus:bg-gray-700" %>
              <%# <% end %>
            </div>
          </div>
        </div>
        <% if user_signed_in? %>
          <div class="block">
            <div class="ml-4 flex items-center md:ml-6">
             <div class="text-white font-bold text-xl"><%= current_user.last_name %>,<%= current_user.first_name %></div>
             <%= link_to "Log out", destroy_user_session_path, :method => :delete , :class => "ml-3 relative bg-blue-600 text-white px-3 py-2 rounded-md font-medium hover:bg-blue-800" %>
            </div>
          </div>
        <% end %>
      </div>
    </div>
  </nav> 
</div>
